[package]
name = "spirit-core"
version = "0.1.0"
edition = "2021"

[dependencies]
blake3 = "1"
ciborium = "0.2"
ed25519-dalek = { version = "3", default-features = false, features = [
  "fast",
  "zeroize",
] }
getrandom = "0.4"
serde = { version = "1", features = ["derive"] }
